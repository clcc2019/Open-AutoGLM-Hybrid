name: Build Android APK

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Grant execute permission for gradlew
      run: chmod +x android-app/gradlew
      
    - name: Build Debug APK
      run: |
        cd android-app
        ./gradlew assembleDebug --stacktrace
        
    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: AutoGLM-Helper-Debug
        path: android-app/app/build/outputs/apk/debug/app-debug.apk
        
    # 检查是否配置了签名密钥（仅在标签触发时）
    - name: Check if signing is configured
      if: startsWith(github.ref, 'refs/tags/')
      id: check_signing
      run: |
        if [ -n "${{ secrets.KEYSTORE_BASE64 }}" ]; then
          echo "has_signing=true" >> $GITHUB_OUTPUT
        else
          echo "has_signing=false" >> $GITHUB_OUTPUT
        fi
        
    # 构建带签名的 Release APK (需要配置 GitHub Secrets)
    - name: Setup Keystore for Release Build
      if: startsWith(github.ref, 'refs/tags/') && steps.check_signing.outputs.has_signing == 'true'
      run: |
        echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 -d > android-app/keystore.jks
        chmod 600 android-app/keystore.jks
        
    - name: Build Signed Release APK
      if: startsWith(github.ref, 'refs/tags/') && steps.check_signing.outputs.has_signing == 'true'
      env:
        KEYSTORE_FILE: android-app/keystore.jks
        KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
        KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
        KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
      run: |
        cd android-app
        ./gradlew assembleRelease --stacktrace
        
    - name: Upload Signed Release APK
      if: startsWith(github.ref, 'refs/tags/') && steps.check_signing.outputs.has_signing == 'true'
      uses: actions/upload-artifact@v4
      with:
        name: AutoGLM-Helper-Release-Signed
        path: android-app/app/build/outputs/apk/release/app-release.apk
        
    # 如果没有配置签名，构建未签名的 Release APK
    - name: Build Unsigned Release APK (on tag)
      if: startsWith(github.ref, 'refs/tags/') && steps.check_signing.outputs.has_signing == 'false'
      run: |
        cd android-app
        ./gradlew assembleRelease --stacktrace
        
    - name: Upload Unsigned Release APK (on tag)
      if: startsWith(github.ref, 'refs/tags/') && steps.check_signing.outputs.has_signing == 'false'
      uses: actions/upload-artifact@v4
      with:
        name: AutoGLM-Helper-Release-Unsigned
        path: android-app/app/build/outputs/apk/release/app-release.apk
        
    # 清理敏感文件
    - name: Clean up keystore
      if: always() && startsWith(github.ref, 'refs/tags/')
      run: |
        rm -f android-app/keystore.jks
